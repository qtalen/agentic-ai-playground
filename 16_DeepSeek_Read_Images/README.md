This repo contains the source code for my latest article [DeepSeek-V4 Can't Read Images? I Made It Read](https://www.dataleadsfuture.com/deepseek-v4-cant-read-images-i-made-it-read).

Put the `observer.md` file into `~/.config/opencode/agents/` directory.

Put the `observer-bridge.js` file into `~/.config/opencode/plugin/` directory.

Then you're good to go.